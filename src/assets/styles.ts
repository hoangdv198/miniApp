import {StyleSheet} from 'react-native';
export const styleBase = StyleSheet.create({
  conainer: {
    flex: 1,
  },
  containerCenter: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
